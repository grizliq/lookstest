# LooksTest — пре-лендинг (lookstest.ru)

Одностраничник под кластер «луксмаксинг»: оценка внешности по фото и советы по
улучшению. CTA ведёт в телеграм-бот [@LooksTestBot](https://t.me/LooksTestBot).

Собран на базе vitrio.pro — те же токены, шапка, кнопки, сетки, подвал, блок
текста и FAQ. Из базы выброшены блоки, которых здесь нет: демо генерации,
логотипы площадок, туториал, промо-акция и тарифы.

## Структура

| Блок | id | Под что |
|---|---|---|
| Hero + демо-разбор | — | «луксмаксинг», «оценка внешности по фото» |
| Как проходит оценка | `#why` | «луксмаксинг по фото» |
| Что смотрит алгоритм | `#feat` | «луксмаксинг лица» |
| Таблица луксмаксинга | `#tiers` | «таблица луксмаксинга» — 16 622 показа |
| Советы по улучшению | `#tips` | «советы по улучшению внешности» |
| Текст + FAQ | `#about` | длинный хвост, разметка FAQPage |

## Демо-разбор в герое

Силуэт лица нарисован SVG, не фотографией: нечего лицензировать, нечего
растягивать и нет вопроса, чьё это лицо. По кнопке едет сканирующая линия,
полосы метрик заполняются, общий балл набегает счётчиком.

Значения лежат в `data-val` на `.scan__row`, балл считается как их среднее
делённое на 10 — если менять цифры, балл пересчитается сам и не разойдётся
с полосами.

Состояние читается из классов `is-running` / `is-done` на `#heroScan`, а не из
переменной в замыкании: иначе оно расходится с DOM, когда прогон обрывают
на середине.

При `prefers-reduced-motion` луч и счётчик отключаются, значения проставляются
сразу — эффект не пропадает, уходит только движение.

## Тексты: что сознательно сделано

Тема граничит с сообществами, где крутятся вредные советы, поэтому:

- шкала в таблице описана как **договорённость сообщества**, а не как
  объективная или медицинская — прямо в подписи под таблицей;
- формулировки уровней нейтральные, без унижающих ярлыков;
- есть отдельный блок и раздел в статье про **опасные практики** (bone smashing,
  голодание ради скул, препараты без назначения) — это и польза читателю,
  и трафик по этим запросам;
- про мьюинг сказано честно: для осанки полезно, ортодонта не заменяет;
- нет обещаний конкретного результата и нет ни одной выдуманной цифры
  про точность алгоритма.

## Ключи

См. `keywords.csv`. Частотности — из Вордстата, скриншот от 07.09.2026.

## Что осталось сделать

1. **Партнёрская метка в ссылке на бота.** Сейчас во всех CTA голый
   `https://t.me/LooksTestBot` без ref-параметра. Как будет метка:
   ```bash
   sed -i 's|https://t.me/LooksTestBot|https://t.me/LooksTestBot?start=РЕФКА|g' index.html
   ```
2. **Метрика** не установлена.
3. **Домен** `lookstest.ru` — свободен на 07.09.2026, но ещё не куплен.

## Публикация

Файлы для GitHub Pages готовы: `CNAME`, `.nojekyll`, `robots.txt`, `sitemap.xml`.

DNS для апекса — A-записи:
```
185.199.108.153
185.199.109.153
185.199.110.153
185.199.111.153
```
плюс CNAME `www` -> `grizliq.github.io`.

Смена домена:
```bash
sed -i 's|lookstest.ru|НОВЫЙ.ru|g' index.html robots.txt sitemap.xml CNAME
```

## Кэш

`bump.py` дописывает к `style.css` и `app.js` хеш содержимого. Сам `index.html`
браузер тоже кэширует, поэтому после `python bump.py` первый раз страницу нужно
открыть с `?nocache=1`, иначе увидите старую разметку со старыми ссылками.
